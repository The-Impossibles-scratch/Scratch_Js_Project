javascript:(function(){const ws=new WebSocket("wss://clouddata.scratch.mit.edu");ws.onopen=()=>{const username= try {document.querySelector('.profile-name').textContent} catch (error){alert("You Must Be Log In");throw new Error(error)};let project_id=prompt("Input Project ID (If nothing is entered, it will automatically be the current Project Id.)");if(!project_id){const url=window.location.href;const projectId=url.match(/projects\/(\d+)/)?.[1];project_id=Number(projectId)}if(typeof project_id!=='number'||isNaN(project_id)){alert("Input Project Id (Number Only)");throw new Error("Input Project Id (Number Only)")}ws.send(JSON.stringify({"method":"handshake","user":username,"project_id":project_id})+"\n");let cloud_var=prompt("Input Cloud Var Name (If nothing is entered, it will automatically be 'My Cloud Var')");let cloud_value=prompt("Input Cloud Value (If nothing is entered, it will automatically be '0')");if(!cloud_value){cloud_value=0}else{cloud_value=Number(cloud_value)}if(!cloud_var){cloud_var="My Cloud Var"}if(typeof cloud_value!=='number'||isNaN(cloud_value)){alert("Input Cloud Value (Number Only)");throw new Error("Input Cloud Value (Number Only)")}alert("Start");ws.send(JSON.stringify({"method":"set","user":username,"project_id":project_id,"name":"☁"+cloud_var,"value":cloud_value})+"\n");alert("FINISH");ws.close()};ws.onclose=()=>{alert("Closed")};})();
