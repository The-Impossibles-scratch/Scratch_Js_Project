javascript:(async function(project_id,limit,offset){const url=`https://clouddata.scratch.mit.edu/logs?projectid=${project_id}&limit=${limit}&offset=${offset}`;try{const res=await fetch(url);const logs=await res.json();if(logs.length==0)return alert("No Cloud Data activity.");const latest={};for(const log of logs){if(!latest[log.name]||latest[log.name].timestamp<log.timestamp)latest[log.name]=log;}const out=Object.values(latest);const blob=new Blob([JSON.stringify(out,null,2)],{type:"application/json"});const a=document.createElement("a");a.href=URL.createObjectURL(blob);a.download=`cloud_data_${project_id}.json`;a.click()}catch(e){alert("Fetch Error : "+e)}})(prompt("Input Project ID")||(function(){const url=location.href;const id=url.match(/projects\/(\d+)/)?.[1];return Number(id)})(),Number(prompt("Input Limit")||1000),Number(prompt("Input Offset")||0));
